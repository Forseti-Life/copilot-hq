# Escalation: dev-infra is blocked

- Website: infrastructure
- Module: 
- Role: software-developer
- Agent: dev-infra
- Item: 20260425-executor-backend-qa-open-source-malformed-responses
- Status: blocked
- Supervisor: pm-infra
- Outbox file: sessions/dev-infra/outbox/20260425-executor-backend-qa-open-source-malformed-responses.md
- Created: 2026-04-25T19:01:29+00:00

## Decision needed
- Should qa-open-source issue needs-info responses, or should it only issue Status: done with BLOCK verdicts when freeze packet is incomplete?


## Recommendation
- Update qa-open-source.instructions.md to explicitly forbid `Status: needs-info` in favor of `Status: done` + BLOCK verdict. All missing information (freeze packet gaps, etc.) should be documented as blockers in the BLOCK verdict, not escalated as needs-info. This keeps qa-open-source in a simple APPROVE/BLOCK loop without ambiguous escalation states. If broader org needs qa-open-source to escalate, that should be explicitly authorized with an example response structure.


## ROI estimate
- ROI: 6
- Rationale: Unblocks qa-open-source from quarantine loop and prevents future cycles. Fixes a single seat's instruction gap rather than a systemic backend issue, so impact is moderate but important for release readiness.

---
- Agent: dev-infra
- Source inbox: /home/ubuntu/forseti.life/sessions/dev-infra/inbox/20260425-executor-backend-qa-open-source-malformed-responses
- Generated: 2026-04-25T19:01:29+00:00

## Needs from Supervisor (up-chain)
- Clarification on whether qa-open-source seat should issue `Status: needs-info` responses at all, or always return `Status: done` with BLOCK verdict + blockers when info is missing. If needs-info is valid, provide an example of properly-structured qa-open-source needs-info response with concrete "Needs from Supervisor" items.


## Blockers
- Incomplete qa-open-source seat instructions. The instructions define the validation flow (read packet, run plan, return APPROVE/BLOCK verdict) but don't document: (1) when needs-info is appropriate for this seat, (2) how to structure a needs-info response with required Needs section, (3) how qa-open-source differs from other QA seats that don't typically issue needs-info.


## Full outbox (context)
I'll investigate the executor backend issue with qa-open-source malformed responses. Let me start by examining the evidence.

Let me examine the malformed outbox files mentioned in the README:

Interesting - these outboxes actually DO have Status headers. Let me check the qa-open-source instructions to understand what might be different:

Now let me check the executor logs or any recent execution details:

The inbox is empty. Let me check what the actual issue is by looking at the CEO cleanup responses and understanding the pattern better:

Let me trace through the actual dispatch history by checking the repository state more carefully. The issue description says qa-open-source is generating responses missing the Status: header, but I see valid ones. Let me check if there's a pattern in the inbox history:

Let me check if there are recent executor dispatch attempts or logs:

I see malformed blockers in the log, but not specific to qa-open-source. Let me check if qa-open-source has been dispatched at all recently:

Now I understand the issue better. Let me check the actual executor dispatch history and understand what "malformed responses" means in the context:

Let me check the daily logs for qa-open-source patterns:

Perfect! Now I see the pattern clearly. The issue is that qa-open-source's response `20260424-validate-phase1-tree-on-commit-5e9f8e553.md` was marked as `MALFORMED: needs-info with empty/N/A Needs section — CEO cleanup needed`.

The problem is NOT that the Status header is missing - it's that the response had `Status: needs-info` but with an empty "Needs from Supervisor" section, which violates the malformed response rule.

Let me verify this by checking that exact file:

Aha! Now I see it. This is a CEO-generated response (evidenced by the "Generated: 2026-04-24" footer and the structure). This file was generated by the executor/CEO after qa-open-source failed to respond properly. The original problem was that qa-open-source itself didn't return a valid response.

Let me check what the original inbox item contained:

Let me check what the actual issue is by understanding the root cause. The README mentions "missing Status: header" but I see Status headers in the responses. Let me trace through the actual problem more carefully by looking at successful vs failed qa seat patterns:

Now I need to understand what the actual underlying problem is. Let me look at whether qa-open-source has seat-specific configuration issues:

Good! So qa-open-source DID succeed once (20260414 Status: done), but then failed. Let me analyze what changed. Let me check the pattern of failures:

The pattern is clear: qa-open-source succeeded once (20260414), then on 20260420 it generated a needs-info response. The executor then quarantined it as malformed, and CEO has been cleaning up repeatedly. This points to a problem where qa-open-source is generating responses, but not in the expected format.

Let me now check the actual root cause by looking at what the requirements are for qa-open-source responses. The key issue is whether this is a seat prompt issue or an executor backend constraint issue:

Based on my investigation, I can now write my findings. The root cause appears to be that qa-open-source is generating `needs-info` responses without properly populated "Needs from Supervisor" sections, which violates the malformed response rule. This isn't actually a backend parsing issue - it's a seat prompt/instructions issue.

- Status: blocked
- Summary: Investigation reveals the issue is NOT executor backend failure or missing Status headers—qa-open-source successfully generates responses with valid Status lines (confirmed across 3 items). The actual problem is qa-open-source generates `Status: needs-info` responses with empty "Needs from Supervisor" sections, which violates the malformed-response rule (per org-wide.instructions.md needs-info validity rule). This causes executor to quarantine after 3 retries. The underlying cause is qa-open-source seat instructions lack clear guidance on when needs-info is appropriate vs when to set Status: blocked with explicit blockers, and lacks examples showing proper "Needs from Supervisor" population for QA validation scope.

## Blockers
- Incomplete qa-open-source seat instructions. The instructions define the validation flow (read packet, run plan, return APPROVE/BLOCK verdict) but don't document: (1) when needs-info is appropriate for this seat, (2) how to structure a needs-info response with required Needs section, (3) how qa-open-source differs from other QA seats that don't typically issue needs-info.

## Needs from CEO
- Clarification on whether qa-open-source seat should issue `Status: needs-info` responses at all, or always return `Status: done` with BLOCK verdict + blockers when info is missing. If needs-info is valid, provide an example of properly-structured qa-open-source needs-info response with concrete "Needs from Supervisor" items.

## Decision needed
- Should qa-open-source issue needs-info responses, or should it only issue Status: done with BLOCK verdicts when freeze packet is incomplete?

## Recommendation
- Update qa-open-source.instructions.md to explicitly forbid `Status: needs-info` in favor of `Status: done` + BLOCK verdict. All missing information (freeze packet gaps, etc.) should be documented as blockers in the BLOCK verdict, not escalated as needs-info. This keeps qa-open-source in a simple APPROVE/BLOCK loop without ambiguous escalation states. If broader org needs qa-open-source to escalate, that should be explicitly authorized with an example response structure.

## ROI estimate
- ROI: 6
- Rationale: Unblocks qa-open-source from quarantine loop and prevents future cycles. Fixes a single seat's instruction gap rather than a systemic backend issue, so impact is moderate but important for release readiness.

---
- Agent: dev-infra
- Source inbox: /home/ubuntu/forseti.life/sessions/dev-infra/inbox/20260425-executor-backend-qa-open-source-malformed-responses
- Generated: 2026-04-25T19:01:29+00:00
